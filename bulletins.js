// ============================================================
// BULLETINS.JS — Comment generation
// ============================================================

function renderBulletins() {
  var container = document.getElementById('module-bulletins');
  if (!container) return;
  container.innerHTML = '<h2>Bulletins</h2><p>Module en construction.</p>';
}
