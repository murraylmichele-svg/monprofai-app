// ============================================================
// PRODUCTIONS.JS — Marks, rubrics, and production notes
// ============================================================

function renderProductions() {
  var container = document.getElementById('module-productions');
  if (!container) return;
  container.innerHTML = '<h2>Productions</h2><p>Module en construction.</p>';
}
